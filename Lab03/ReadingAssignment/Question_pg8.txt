- Question: 
If you create a constructor method to build a DVD by title then create a constructor method to build 
a DVD by category. Does JAVA allow you to do this?

- Answer: 
Yes, in Java we are allowed to do this and we can call these constructor methods in anyway we want, by title or 
by category at a same application.